---
title: إحصائيات
---

## API الإحصائيات المتقدم

### 📊 الإحصائيات العامة
```http
GET /api/stats
```

**الاستجابة:**
```json
{
  "total_tickets": 1250,
  "open_tickets": 45,
  "closed_tickets": 1205,
  "average_response_time": "2.5 hours",
  "average_resolution_time": "24 hours",
  "satisfaction_rating": 4.7,
  "total_users": 850,
  "active_users": 120
}
```

### 📅 الإحصائيات اليومية
```http
GET /api/stats/daily
```

**المعاملات:**
- `date` - التاريخ المحدد (YYYY-MM-DD)
- `days` - عدد الأيام (افتراضي: 7)

**مثال:**
```javascript
// إحصائيات آخر 7 أيام
const dailyStats = await fetch('/api/stats/daily?days=7', {
  headers: { 'Authorization': 'Bearer YOUR_TOKEN' }
});

// إحصائيات يوم محدد
const specificDay = await fetch('/api/stats/daily?date=2024-01-15', {
  headers: { 'Authorization': 'Bearer YOUR_TOKEN' }
});
```

### 📈 الإحصائيات الأسبوعية
```http
GET /api/stats/weekly
```

**المعاملات:**
- `week` - رقم الأسبوع
- `year` - السنة
- `weeks` - عدد الأسابيع (افتراضي: 4)

**مثال:**
```javascript
// إحصائيات آخر 4 أسابيع
const weeklyStats = await fetch('/api/stats/weekly?weeks=4', {
  headers: { 'Authorization': 'Bearer YOUR_TOKEN' }
});
```

### 📊 الإحصائيات الشهرية
```http
GET /api/stats/monthly
```

**المعاملات:**
- `month` - الشهر (1-12)
- `year` - السنة
- `months` - عدد الأشهر (افتراضي: 6)

**مثال:**
```javascript
// إحصائيات آخر 6 أشهر
const monthlyStats = await fetch('/api/stats/monthly?months=6', {
  headers: { 'Authorization': 'Bearer YOUR_TOKEN' }
});
```

## أنواع الإحصائيات

### 🎫 إحصائيات التذاكر
- **إجمالي التذاكر**: عدد التذاكر الكلي
- **التذاكر المفتوحة**: التذاكر النشطة حالياً
- **التذاكر المغلقة**: التذاكر المحلولة
- **معدل الإغلاق**: نسبة التذاكر المحلولة
- **التذاكر حسب الأولوية**: توزيع حسب الأولوية
- **التذاكر حسب الفئة**: توزيع حسب النوع

### ⏱️ إحصائيات الوقت
- **متوسط وقت الاستجابة**: الوقت للرد الأول
- **متوسط وقت الحل**: الوقت لحل التذكرة
- **أسرع حل**: أسرع تذكرة تم حلها
- **أطول حل**: أطول تذكرة استغرقت
- **توزيع أوقات الحل**: توزيع حسب فترات زمنية

### 👥 إحصائيات المستخدمين
- **إجمالي المستخدمين**: عدد المستخدمين المسجلين
- **المستخدمين النشطين**: المستخدمين النشطين
- **معدل الاستخدام**: متوسط التذاكر لكل مستخدم
- **أكثر المستخدمين نشاطاً**: المستخدمين الأكثر طلباً للدعم

### ⭐ إحصائيات التقييم
- **متوسط التقييم**: متوسط تقييم الخدمة
- **توزيع التقييمات**: توزيع حسب النجوم
- **معدل الرضا**: نسبة المستخدمين الراضين
- **التقييمات حسب الفئة**: تقييمات حسب نوع التذكرة

## أمثلة متقدمة

### تحليل الاتجاهات
```javascript
// مقارنة الأداء بين فترتين
const currentMonth = await fetch('/api/stats/monthly?months=1', {
  headers: { 'Authorization': 'Bearer YOUR_TOKEN' }
});

const previousMonth = await fetch('/api/stats/monthly?months=1&offset=1', {
  headers: { 'Authorization': 'Bearer YOUR_TOKEN' }
});

// حساب النمو
const growth = (currentMonth.total_tickets - previousMonth.total_tickets) / previousMonth.total_tickets * 100;
```

### إحصائيات مخصصة
```javascript
// إحصائيات لفترة محددة
const customStats = await fetch('/api/stats?date_from=2024-01-01&date_to=2024-01-31', {
  headers: { 'Authorization': 'Bearer YOUR_TOKEN' }
});
```

### تصدير البيانات
```javascript
// تصدير الإحصائيات كـ CSV
const exportStats = await fetch('/api/stats/export?format=csv&period=monthly', {
  headers: { 'Authorization': 'Bearer YOUR_TOKEN' }
});

const csvData = await exportStats.text();
```

## رموز الاستجابة

### ✅ نجح العملية
- **200 OK**: طلب ناجح
- **206 Partial Content**: بيانات جزئية

### ❌ فشل العملية
- **400 Bad Request**: معاملات غير صحيحة
- **401 Unauthorized**: غير مصرح
- **403 Forbidden**: ممنوع الوصول
- **429 Too Many Requests**: تجاوز حد الطلبات

## أفضل الممارسات

### 📊 تحليل البيانات
- **استخدم الفلاتر**: ركز على البيانات المطلوبة
- **قارن الفترات**: فهم الاتجاهات والتغييرات
- **راقب المؤشرات**: تتبع مؤشرات الأداء الرئيسية
- **تصدير البيانات**: حفظ البيانات للمراجعة

### 🔧 التطوير
- **استخدم التخزين المؤقت**: تحسين الأداء
- **تحقق من الأخطاء**: تعامل مع جميع الحالات
- **استخدم التجميع**: تقليل عدد الطلبات
- **راقب الاستخدام**: تتبع استخدام API
